apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: uo-prod

# Production secrets are NOT managed by ArgoCD
# Create secrets manually using:
#   1. create-prod-secrets.sh script
#   2. Or kubectl create secret commands
# See README.md for instructions

resources: []
