# Production secrets should NEVER be committed to Git
# Only templates and documentation should exist in this directory
*sealed.yaml
*secret.yaml
*.pem

# Allow README and template files
!README.md
!*-template.yaml
!create-prod-secrets.sh
