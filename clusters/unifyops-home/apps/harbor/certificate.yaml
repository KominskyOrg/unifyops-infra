apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-unifyops-io
  namespace: harbor-system
spec:
  secretName: harbor-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
    - harbor.unifyops.io
