apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: unifyops
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dns
spec:
  ingressClassName: traefik
  tls:
    - hosts: ["api.dev.unifyops.io"]
      secretName: unifyops-dev-tls
  rules:
    - host: api.dev.unifyops.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: unifyops
                port:
                  number: 80